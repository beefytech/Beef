# Checks that modules keep their interface slot dependency across compiles, without the
# debugger: the program asserts the results of its virtual calls and is run with Execute,
# which fails the script if the process exits non-zero. The second compile grows the
# interface slot count; with stale vdata offsets the rebuilt program crashes before Main.

ShowFile("src/Virtuals.bf")

Compile()
Execute("$(TargetPath)")

ToggleCommentAt("ClassA_IFaces")
ToggleCommentAt("Test_IFaces")
Compile()
Execute("$(TargetPath)")
